# 🌐 مترجم فوري متعدد اللغات

تطبيق سطح مكتب لنظام Windows يترجم تلقائياً أي نص منسوخ إلى العربية (أو لغات أخرى) باستخدام API مجاني من LibreTranslate.

## ✨ المميزات

- **مراقبة الحافظة تلقائياً**: يكتشف ويترجم النص فور نسخه
- **كشف اللغة الذكي**: يكتشف لغة النص المصدر تلقائياً
- **ترجمة ذكية**: يترجم أي لغة (ماعدا اللغة المستهدفة) إلى اللغة المختارة
- **تخطي نفس اللغة**: لن يترجم إذا كان النص بالفعل باللغة المستهدفة
- **الترجمة الافتراضية للعربية**: يترجم إلى العربية افتراضياً
- **دعم متعدد اللغات**: التبديل بين أكثر من 10 لغات (العربية، الإنجليزية، الإسبانية، الفرنسية، الألمانية، الروسية، الصينية، اليابانية، الإيطالية، البرتغالية)
- **كشف النص الذكي**: يتخطى تلقائياً الروابط ومقاطع الكود
- **الحفاظ على التنسيق**: يحافظ على فواصل الأسطر، علامات الترقيم، الأرقام، والأحرف الخاصة
- **عرض فوري منبثق**: يعرض الترجمات فوراً في نافذة منبثقة مريحة
- **عرض معلومات اللغة**: يعرض لغة المصدر واللغة المستهدفة بوضوح
- **API مجاني**: يستخدم API مجاني من LibreTranslate (لا يتطلب مفتاح API مدفوع)
- **تتبع الإحصائيات**: يحتفظ بعدد الترجمات المنفذة
- **واجهة ثنائية اللغة**: تسميات بالعربية والإنجليزية لسهولة الاستخدام

## 📋 المتطلبات

- نظام Windows
- Python 3.7 أو أحدث
- اتصال بالإنترنت (لاستدعاءات API)

## 🚀 التثبيت

### الخطوة 1: تثبيت Python

إذا لم يكن لديك Python مثبت:
1. قم بتنزيل Python من [python.org](https://www.python.org/downloads/)
2. أثناء التثبيت، **ضع علامة على "Add Python to PATH"**
3. أكمل التثبيت

### الخطوة 2: تثبيت المكتبات المطلوبة

افتح Command Prompt أو PowerShell في مجلد المشروع وقم بتشغيل:

```bash
pip install -r requirements.txt
```

سيتم تثبيت:
- `pyperclip` - لمراقبة الحافظة
- `requests` - للاتصال بـ API

### الخطوة 3: اختبار API (اختياري)

قبل تشغيل التطبيق الرئيسي، يمكنك اختبار ما إذا كان API يعمل:

```bash
python test_translator.py
```

سيختبر هذا الترجمات من لغات مختلفة للتحقق من اتصال API.

## 🎯 الاستخدام

### البدء السريع (Windows)

**الخيار 1: استخدام ملف Batch (الأسهل)**
1. انقر نقراً مزدوجاً على `install_dependencies.bat` (المرة الأولى فقط)
2. انقر نقراً مزدوجاً على `run_translator.bat` لبدء التطبيق

**الخيار 2: استخدام سطر الأوامر**
1. افتح Command Prompt أو PowerShell في مجلد المشروع
2. قم بتشغيل التطبيق:

```bash
python translator.py
```

### استخدام المترجم

1. **انقر على "▶ Start Monitoring"** لبدء مراقبة الحافظة
2. **انسخ أي نص** (Ctrl+C) من أي مكان على جهاز الكمبيوتر
3. **ستظهر نافذة منبثقة** مع الترجمة
4. **غيّر اللغة المستهدفة** من القائمة المنسدلة إذا لزم الأمر
5. **انقر على "⏸ Stop Monitoring"** للإيقاف المؤقت

### الميزات أثناء العمل

- **نسخ الترجمة**: انقر على زر "📋 نسخ الترجمة / Copy" في النافذة المنبثقة لنسخ النص المترجم
- **الإغلاق التلقائي**: تغلق النوافذ المنبثقة تلقائياً بعد 15 ثانية
- **الإحصائيات**: عرض عدد الترجمات المنفذة
- **اختيار اللغة**: اختر من بين أكثر من 10 لغات مدعومة

## 🔧 التكوين

يمكنك تعديل الإعدادات التالية في `config.py`:

```python
# السطر 11
DEFAULT_TARGET_LANGUAGE = "ar"  # اللغة المستهدفة الافتراضية (ar = العربية)
CHECK_INTERVAL = 0.5  # فترة فحص الحافظة بالثواني
```

## 🌍 اللغات المدعومة

- العربية (Arabic)
- الإنجليزية (English)
- الإسبانية (Spanish)
- الفرنسية (French)
- الألمانية (German)
- الروسية (Russian)
- الصينية (Chinese)
- اليابانية (Japanese)
- الإيطالية (Italian)
- البرتغالية (Portuguese)
- التركية (Turkish)
- الكورية (Korean)
- الهندية (Hindi)
- الهولندية (Dutch)
- البولندية (Polish)

## 🛡️ الميزات الذكية

### ما يتم ترجمته
- محتوى النص العادي
- الجمل والفقرات
- المحتوى المختلط (نص مع روابط - يتم ترجمة النص فقط)

### ما يتم تخطيه
- الروابط (URLs) - يتم الحفاظ عليها كما هي
- مقاطع الكود (يتم اكتشافها بأنماط مثل `def`، `class`، `function`، إلخ)
- النص القصير جداً (أقل من حرفين)
- سلاسل URL النقية
- النص الذي بنفس اللغة المستهدفة

## 📝 أمثلة على المخرجات

### مثال 1: الإنجليزية إلى العربية
عند نسخ: `"Hello, how are you?"`

تظهر النافذة المنبثقة:
```
🔄 English ➜ Arabic (العربية)

📝 النص الأصلي (English):
Hello, how are you?

✅ الترجمة (Arabic):
مرحبا، كيف حالك؟
```

### مثال 2: الإسبانية إلى العربية
عند نسخ: `"Hola, ¿cómo estás?"`

تظهر النافذة المنبثقة:
```
🔄 Spanish (Español) ➜ Arabic (العربية)

📝 النص الأصلي (Spanish):
Hola, ¿cómo estás?

✅ الترجمة (Arabic):
مرحبا، كيف حالك؟
```

### مثال 3: نفس اللغة (تم التخطي)
عند نسخ نص عربي بينما العربية هي اللغة المستهدفة:

تظهر النافذة المنبثقة:
```
ℹ️ النص بالفعل باللغة المستهدفة (Arabic)
```
(تغلق تلقائياً بعد 5 ثوانٍ)

## 🔍 استكشاف الأخطاء وإصلاحها

### خطأ "Module not found"
```bash
pip install -r requirements.txt
```

### "خطأ في الاتصال"
- تحقق من اتصالك بالإنترنت
- تحقق من إمكانية الوصول إلى https://libretranslate.com
- قد يكون لدى API المجاني حدود معدل؛ انتظر لحظة وحاول مرة أخرى

### الحافظة لا تعمل
- تأكد من عدم تداخل مديري الحافظة الآخرين
- أعد تشغيل التطبيق
- تحقق من أن لديك أذونات الوصول إلى الحافظة

### الترجمة لا تظهر
- تأكد من النقر على "Start Monitoring"
- تحقق من أن النص المنسوخ ليس رابط URL أو كود
- تحقق من أن النص يحتوي على حرفين على الأقل
- تحقق من أن النص ليس بنفس اللغة المستهدفة

## 🎨 التخصيص

يمكنك تخصيص التطبيق عن طريق تحرير `config.py`:

### تغيير اللغة الافتراضية
```python
DEFAULT_TARGET_LANGUAGE = "en"  # تغيير إلى الإنجليزية
```

### ضبط مدة النافذة المنبثقة
```python
POPUP_AUTO_CLOSE_TIME = 20000  # 20 ثانية (بالملي ثانية)
SKIP_MESSAGE_AUTO_CLOSE_TIME = 3000  # 3 ثوانٍ
```

### تغيير أحجام النوافذ
```python
MAIN_WINDOW_WIDTH = 450
MAIN_WINDOW_HEIGHT = 550
POPUP_WINDOW_WIDTH = 600
POPUP_WINDOW_HEIGHT = 450
```

### ضبط سرعة فحص الحافظة
```python
CHECK_INTERVAL = 0.3  # فحص كل 0.3 ثانية (أسرع لكن يستخدم المزيد من CPU)
```

### إضافة المزيد من اللغات
```python
SUPPORTED_LANGUAGES = [
    ("Arabic (العربية)", "ar"),
    ("English", "en"),
    # أضف لغتك هنا:
    ("Swedish (Svenska)", "sv"),
]
```

## 🔒 الخصوصية والأمان

- تتم معالجة جميع الترجمات من خلال API الخاص بـ LibreTranslate
- لا يتم تخزين أي بيانات محلياً أو تسجيلها
- يتم قراءة محتوى الحافظة فقط عند تنشيط المراقبة
- يمكنك إيقاف المراقبة في أي وقت

## 📦 هيكل المشروع

```
trinsloit/
│
├── translator.py              # ملف التطبيق الرئيسي
├── config.py                  # إعدادات التكوين (قابلة للتخصيص)
├── test_translator.py         # سكريبت اختبار API
├── requirements.txt           # مكتبات Python المطلوبة
├── run_translator.bat         # سكريبت البدء السريع (Windows)
├── install_dependencies.bat   # مثبت المكتبات (Windows)
├── README.md                 # التعليمات بالإنجليزية
└── README_AR.md              # هذا الملف (التعليمات بالعربية)
```

## 🔄 كيف يعمل

1. **مراقبة الحافظة**: يراقب التطبيق باستمرار الحافظة بحثاً عن نص جديد
2. **كشف اللغة**: عند نسخ النص، يكتشف لغة المصدر باستخدام API الكشف من LibreTranslate
3. **منطق الترجمة الذكي**:
   - إذا كانت لغة المصدر = اللغة المستهدفة → تخطي الترجمة (عرض رسالة معلومات)
   - إذا كانت لغة المصدر ≠ اللغة المستهدفة → الترجمة إلى اللغة المستهدفة
4. **العرض الفوري**: يعرض نافذة منبثقة مع الترجمة ولغة المصدر واللغة المستهدفة
5. **الإغلاق التلقائي**: تغلق النافذة المنبثقة تلقائياً بعد 15 ثانية (5 ثوانٍ لرسائل التخطي)

## 💡 نصائح

1. **أبقِ النافذة الرئيسية مفتوحة** أثناء المراقبة (يمكنك تصغيرها)
2. **استخدم اختصارات لوحة المفاتيح** (Ctrl+C) للنسخ السريع
3. **غيّر اللغات أثناء التشغيل** دون إعادة التشغيل
4. **انسخ الترجمات** مباشرة من النافذة المنبثقة لإعادة الاستخدام
5. **خصص الإعدادات** في `config.py` حسب تفضيلاتك

## 🙏 الشكر والتقدير

- **LibreTranslate**: API ترجمة مجاني ومفتوح المصدر
- **pyperclip**: وحدة حافظة متعددة المنصات
- **tkinter**: مكتبة واجهة المستخدم الرسومية القياسية في Python

---

**استمتع بالترجمات التلقائية! 🎉**

للحصول على الدعم أو الإبلاغ عن المشكلات، يرجى زيارة صفحة المشروع.
