# 🔧 دليل حل المشاكل / Troubleshooting Guide

## ❌ مشكلة: "فشل الترجمة" / "Translation Failed"

### الأسباب المحتملة:

#### 1️⃣ مشكلة في الاتصال بالإنترنت
**الحل:**
- تحقق من اتصالك بالإنترنت
- تأكد من أن الجدار الناري لا يحجب التطبيق
- حاول فتح https://libretranslate.com في المتصفح

#### 2️⃣ تجاوز حد الاستخدام (Rate Limit)
**الأعراض:**
- رسالة "تم تجاوز حد الاستخدام"
- الترجمة تعمل ثم تتوقف

**الحل:**
```
✅ انتظر 1-2 دقيقة ثم حاول مرة أخرى
✅ التطبيق يستخدم API احتياطي تلقائياً (MyMemory)
✅ قلل من عدد الترجمات المتتالية
```

#### 3️⃣ الخادم مشغول أو معطل
**الحل:**
- التطبيق سيحاول استخدام API بديل (MyMemory) تلقائياً
- إذا استمرت المشكلة، انتظر بضع دقائق

#### 4️⃣ مشكلة في كشف اللغة
**الأعراض:**
- النص قصير جداً (أقل من حرفين)
- النص يحتوي على رموز فقط

**الحل:**
```
✅ تأكد من أن النص يحتوي على حرفين على الأقل
✅ تأكد من أن النص ليس رموز أو أرقام فقط
```

---

## 🔍 خطوات التشخيص

### الخطوة 1: اختبار API
```bash
python test_translator.py
```

**النتيجة المتوقعة:**
```
✅ Detected language: en
✅ Translation: مرحبا، كيف حالك؟
```

**إذا فشل الاختبار:**
- تحقق من الاتصال بالإنترنت
- تحقق من إعدادات الجدار الناري

### الخطوة 2: تفعيل وضع التصحيح
افتح Command Prompt وشغّل:
```bash
python translator.py
```

**راقب الرسائل في الكونسول:**
```
Detected language: en
Sending request: {...}
Response status: 200
```

### الخطوة 3: تحقق من الإعدادات
افتح `config.py` وتأكد من:
```python
USE_FALLBACK_API = True  # يجب أن تكون True
```

---

## 🛠️ الحلول السريعة

### الحل 1: استخدام API الاحتياطي فقط
في `config.py`، غيّر:
```python
# استخدم MyMemory API كخيار أساسي
API_URL = "https://api.mymemory.translated.net/get"
USE_FALLBACK_API = False
```

### الحل 2: زيادة وقت الانتظار
في `config.py`، غيّر:
```python
# زيادة فترة فحص الحافظة لتقليل الضغط على API
CHECK_INTERVAL = 1.0  # بدلاً من 0.5
```

### الحل 3: إعادة تثبيت المكتبات
```bash
pip uninstall pyperclip requests
pip install -r requirements.txt
```

---

## 📊 رسائل الخطأ الشائعة

### "انتهت مهلة الترجمة"
**السبب:** الاتصال بطيء أو الخادم لا يستجيب  
**الحل:** 
- تحقق من سرعة الإنترنت
- حاول مرة أخرى بعد قليل
- التطبيق سيحاول API بديل تلقائياً

### "خطأ في الاتصال"
**السبب:** لا يوجد اتصال بالإنترنت  
**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من إعدادات الجدار الناري
- تأكد من عدم استخدام VPN يحجب الخدمة

### "النص بالفعل باللغة المستهدفة"
**السبب:** هذا ليس خطأ! النص المنسوخ بنفس اللغة المختارة  
**الحل:**
- غيّر اللغة المستهدفة من القائمة المنسدلة
- أو انسخ نص بلغة مختلفة

### "تم تجاوز حد الاستخدام"
**السبب:** استخدمت API كثيراً في وقت قصير  
**الحل:**
- انتظر 1-2 دقيقة
- التطبيق سيستخدم API بديل تلقائياً
- قلل من عدد الترجمات المتتالية

---

## 🔄 إعادة تعيين التطبيق

إذا استمرت المشاكل، جرب:

### 1. إيقاف وإعادة تشغيل
```
1. اضغط "Stop Monitoring"
2. أغلق التطبيق
3. افتح التطبيق مرة أخرى
4. اضغط "Start Monitoring"
```

### 2. حذف ملفات مؤقتة
```bash
# في مجلد المشروع
del __pycache__ /s /q
```

### 3. إعادة تثبيت كاملة
```bash
pip uninstall pyperclip requests
pip install -r requirements.txt
python translator.py
```

---

## 🌐 اختبار الاتصال بـ API

### اختبار LibreTranslate
افتح المتصفح واذهب إلى:
```
https://libretranslate.com
```

إذا لم يفتح الموقع، الخدمة قد تكون معطلة مؤقتاً.

### اختبار MyMemory (API الاحتياطي)
افتح المتصفح واذهب إلى:
```
https://api.mymemory.translated.net/get?q=hello&langpair=en|ar
```

يجب أن ترى استجابة JSON مع الترجمة.

---

## 💡 نصائح لتجنب المشاكل

### ✅ افعل:
- استخدم نصوص واضحة (ليست رموز أو أكواد)
- انتظر ثانية بين الترجمات المتتالية
- أبقِ `USE_FALLBACK_API = True` في config.py
- تحقق من الاتصال بالإنترنت قبل البدء

### ❌ لا تفعل:
- لا تنسخ نصوص طويلة جداً (أكثر من 500 كلمة)
- لا تترجم عشرات النصوص في ثانية واحدة
- لا تستخدم VPN قد يحجب الخدمة
- لا تغلق النافذة الرئيسية أثناء التشغيل

---

## 📞 الحصول على مساعدة إضافية

### 1. تحقق من السجلات
عند تشغيل التطبيق من Command Prompt، ستظهر رسائل مفيدة:
```
Detected language: en
Sending request: {...}
Response status: 200
```

### 2. اختبر API يدوياً
استخدم `test_translator.py`:
```bash
python test_translator.py
```

### 3. راجع التوثيق
- `README_AR.md` - التوثيق الكامل
- `QUICK_START.md` - دليل البدء السريع
- `PROJECT_SUMMARY.md` - ملخص المشروع

---

## 🔧 إعدادات متقدمة

### تغيير API الأساسي
في `config.py`:
```python
# استخدم خدمة أخرى
API_URL = "https://your-translation-service.com/translate"
```

### تعطيل API الاحتياطي
في `config.py`:
```python
USE_FALLBACK_API = False  # لن يستخدم MyMemory
```

### زيادة وقت الانتظار
في `translator.py`، ابحث عن:
```python
response = requests.post(self.api_url, json=payload, timeout=10)
```
غيّر `timeout=10` إلى `timeout=20` للاتصالات البطيئة.

---

## ✅ قائمة التحقق النهائية

قبل طلب المساعدة، تأكد من:

- [ ] الاتصال بالإنترنت يعمل
- [ ] تم تثبيت المكتبات: `pip install -r requirements.txt`
- [ ] `USE_FALLBACK_API = True` في config.py
- [ ] النص المنسوخ ليس رمز أو URL
- [ ] النص يحتوي على حرفين على الأقل
- [ ] جربت إعادة تشغيل التطبيق
- [ ] جربت `python test_translator.py`

---

**إذا استمرت المشكلة بعد كل هذه الخطوات، قد تكون الخدمة معطلة مؤقتاً. حاول مرة أخرى لاحقاً.** 🙏
